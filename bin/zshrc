#!/bin/bash

vim -p ~/.dotfiles/zsh/.zshrc ~/.dotfiles/zsh/.zshenv && \
  ln -fs ~/.dotfiles/zsh/.zshrc ~/ && \
  ln -fs ~/.dotfiles/zsh/.zshenv ~/ && \
  ln -fs ~/.dotfiles/zsh/.zprofile ~/ && \
  exec zsh
